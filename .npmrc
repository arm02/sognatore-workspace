# NPM Configuration for @sognatore organization
@sognatore:registry=https://registry.npmjs.org/
//registry.npmjs.org/:_authToken=${NPM_TOKEN}

# Optional: Set npm registry for all packages (if needed)
# registry=https://registry.npmjs.org/

# Publishing configuration
publish-config.access=public
publish-config.registry=https://registry.npmjs.org/